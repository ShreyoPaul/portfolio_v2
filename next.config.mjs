/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'picsum.photos', 'cdn.iconscout.com', 'res.cloudinary.com', 'imgs.search.brave.com'],
    },
};

export default nextConfig;
